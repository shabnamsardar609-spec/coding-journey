#include <studio.h>
int main(){
    printf("hello world");
    return 0;
}

#include <studio.h>
int main(){
    int a;// variable declaration
    a=1;// variable initialization
    printf("%d",a);
    return 0;
}

#include <studio.h>
int main(){
    printf("size of int: %zu bytes\n", sizeof(int));
    printf("size of float: %zu bytes\n", sizeof(float));
    printf("size of char: %zu bytes\n", sizeos(char));
    return 0;
}

#include <studio.h>
int main(){
    int a;
    char ch = 'a'
    ch = '\n' //represents a new line
    printf("hey i am good \n nice");    #hey i am good 
                                         nice        
    printf("hey i am good \t nice");    #hey i am good    nice
    printf("hey i am good \\n nice");   #hey i am good \n nice
    return 0;
}


#includ <stdio.h>
int main(){
    int length = 3;
    int breadth = 6;
    printf("the area of the rectangular is %d", length*breadth);
    return 0;
}
#the area of the rectangular is 18


#include <stdio.h>
int main(){
    //int length = 3
    //int breadth =6
    int length, breadth;
    printf("enter length\n");
    scanf("%d", &length);

    printf("enter breadth\n");
    scanf("%d", &breadth);

    printf("the area of this rectangular is %d", length*breadth);
    return 0;
}

#include <stdio.h>
int main(){
    int r = 6;
    int h = 10;
    printf("the area of the circle with radious %d is %f\n", r, 3.14*r*r);
    printf("the area of the cylinder with radious %d and hight %d is %f", r , h, 3.14*r*r*h);
    return 0;
}

#include <stdio.h>
int main(){ 
    float = 37.0,f;
    f=(9.5/5.0)*c +32;
    printf("the value in fahrenheit is %f, f);
    return 0;
}

#include <stdio.h>
int main(){
    float p = 34.1;
    int r = 8;
    int t = 5;
    printf("the value of simple interest is %f",(p*r*t)\100);
    return 0;
}

#include <stdio.h>
unt main(){
    int i = 10;
// declare and initialize 'i' with 10
    int j = i;
    // %d is called foemat specifier
    // %d is for int , %f is for float and %c is for char
    printf("the value of i is %d and value of j is %d", i,j);
    return 0;
}

#include <stdio.h>
int main(){
    int i =10;
    int j =i;
    int a=2,b=3, c=4, d=5;
    printf("the value of i if %d and value of j is %d\n",i,j);
    printf("the value of a if %d and value of b is %d\n",a,b);
    printf("the value of c if %d and value of d is %d\n",c,d);
    return 0;
}
